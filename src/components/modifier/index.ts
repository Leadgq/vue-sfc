import { sfc } from "@/types/sfc";

export default {
  id: 8,
  name: "自动更新",
  componentName: "Modifier",
  desc: "测试组件的v-model:value、自动更新外界的值、修饰符(这类似于.trim .lazy)、使用withDefaults提供默认值(这个方式支持ts)"
} as sfc;
